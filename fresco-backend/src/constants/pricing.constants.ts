/**
 * Pricing-related constants shared across the application.
 */

/** Minimum permitted price value for a garment service pricing. */
export const PRICE_MIN_VALUE = 0;

/** Default currency for pricing entries. */
export const DEFAULT_CURRENCY = "INR";

/** Default active status assigned to new pricing entries. */
export const DEFAULT_PRICING_STATUS = true;
