/**
 * Category-related constants shared across the application.
 */

/** Minimum length required for a category name. */
export const CATEGORY_NAME_MIN_LENGTH = 2;

/** Maximum length permitted for a category name. */
export const CATEGORY_NAME_MAX_LENGTH = 100;

/** Maximum length permitted for a category description. */
export const CATEGORY_DESCRIPTION_MAX_LENGTH = 500;

/** Default display order assigned to new categories. */
export const DEFAULT_DISPLAY_ORDER = 0;

/** Default active status assigned to new categories. */
export const DEFAULT_CATEGORY_STATUS = true;
