/**
 * Cart-related constants shared across the application.
 */

/** Minimum permitted quantity for a single item in a cart. */
export const MIN_CART_ITEM_QUANTITY = 1;

/** Default total amount for a newly initialized cart. */
export const DEFAULT_CART_TOTAL = 0;

/** Default active status assigned to new carts. */
export const DEFAULT_CART_STATUS = true;
