/**
 * Service-related constants shared across the application.
 */

/** Minimum length required for a service name. */
export const SERVICE_NAME_MIN_LENGTH = 2;

/** Maximum length permitted for a service name. */
export const SERVICE_NAME_MAX_LENGTH = 100;

/** Maximum length permitted for a service description. */
export const SERVICE_DESCRIPTION_MAX_LENGTH = 500;

/** Default display order assigned to new services. */
export const DEFAULT_SERVICE_DISPLAY_ORDER = 0;

/** Default active status assigned to new services. */
export const DEFAULT_SERVICE_STATUS = true;
