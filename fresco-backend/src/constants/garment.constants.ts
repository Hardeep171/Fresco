/**
 * Garment-related constants shared across the application.
 */

/** Minimum length required for a garment name. */
export const GARMENT_NAME_MIN_LENGTH = 2;

/** Maximum length permitted for a garment name. */
export const GARMENT_NAME_MAX_LENGTH = 100;

/** Maximum length permitted for a garment description. */
export const GARMENT_DESCRIPTION_MAX_LENGTH = 500;

/** Default display order assigned to new garments. */
export const DEFAULT_GARMENT_DISPLAY_ORDER = 0;

/** Default active status assigned to new garments. */
export const DEFAULT_GARMENT_STATUS = true;
